# Copia este archivo como .env y rellena los valores
# cp .env.example .env

# Clave secreta para firmar los tokens JWT
# Genera una segura con: openssl rand -hex 32
JWT_SECRET_KEY=cambia_esto_por_una_clave_segura_y_larga
